flask==3.0.3
flask-cors==4.0.1
flask-limiter==3.8.0
flask-talisman==1.1.0
python-dotenv==1.0.1
transformers==4.44.0
tokenizers==0.19.1
requests==2.32.3
bleach==6.1.0
gunicorn==22.0.0

# NOTE: PyTorch is NOT listed here because the correct version depends on your OS.
# Install it separately AFTER running: pip install -r requirements.txt
#
# Windows and Linux (CPU-only):
#   pip install torch --index-url https://download.pytorch.org/whl/cpu
#
# macOS (Apple Silicon or Intel):
#   pip install torch
